import {Text, View} from "react-native";

export const SeeAllMoviesScreen = () => {
    return (<View style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center'
    }}>
        <Text>This is SeeAllMovies Screen fo the app.</Text>
    </View>)
}
